#include "syscall.h"

int main ()
{
    /*PutString("I want to open the file!!\n");
    OpenFileId fileId = Open("outputtest");

    int size = 22;
    char buffer[size];
    Read(buffer, size, fileId);
    Close(fileId);
    PutString(buffer);*/
    End();
    /* not reached */
    return 0;
}